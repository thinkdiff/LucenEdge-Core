import ServicesClient from './ServicesClient'
import type { Metadata } from 'next'
export const metadata: Metadata = { title: 'Services — LucenEdge' }
export default function ServicesPage() { return <ServicesClient /> }
