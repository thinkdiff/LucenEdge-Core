import CasesClient from './CasesClient'
import type { Metadata } from 'next'
export const metadata: Metadata = { title: 'Case Studies — LucenEdge' }
export default function CasesPage() { return <CasesClient /> }
