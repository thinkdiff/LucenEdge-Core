import ContactClient from './ContactClient'
import type { Metadata } from 'next'
export const metadata: Metadata = { title: 'Contact — LucenEdge' }
export default function ContactPage() { return <ContactClient /> }
